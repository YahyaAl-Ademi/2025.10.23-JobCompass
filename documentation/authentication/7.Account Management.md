**Phase 7: Account Management**

- [← Back to Authentication Flowchart](_AuthenticationFlowchart.md)
- [← Previous: Phase 6: Password Reset](6.Password%20Reset.md)
- [Next → Phase 8: Security Features](8.Security%20Features.md)

```mermaid
sequenceDiagram
    autonumber
    actor User
    %% File paths:
    %% ProfileSettings: client/src/components/ProfileSettings/ProfileSettings.jsx
    %% SkillsSettings: client/src/components/SkillsSettings/SkillsSettings.jsx
    %% updateProfile: server/src/controllers/user.js
    %% changeSkills: server/src/controllers/changeSkills.js
    %% changePassword: server/src/controllers/changePassword.js
    %% deleteUser: server/src/controllers/deleteUser.js
    participant ProfileSettings as Profile Settings Component<br/>ProfileSettings.jsx
    participant SkillsSettings as Skills Settings Component<br/>SkillsSettings.jsx
    participant ProfileAPI as Profile Controller<br/>user.js
    participant SkillsAPI as Skills Controller<br/>changeSkills.js
    participant PasswordAPI as Password Controller<br/>changePassword.js
    participant DeleteAPI as Delete Account Controller<br/>deleteUser.js

    Note over User,DeleteAPI: Phase 7: Account Management Operations

    User->>ProfileSettings: Navigates to profile settings
    activate ProfileSettings

    Note over ProfileSettings,ProfileAPI: Profile Update Operation
    User->>ProfileSettings: Updates profile fields<br/>(name, address, etc.)
    ProfileSettings->>ProfileSettings: Client-side validation
    ProfileSettings->>ProfileAPI: PUT /api/users/profile<br/>body: { first_name, last_name, street, house_number, city, country }
    activate ProfileAPI

    ProfileAPI->>ProfileAPI: Validate input fields
    ProfileAPI->>ProfileAPI: Update user profile in database
    deactivate ProfileAPI

    ProfileAPI->>ProfileSettings: Return updated user data
    ProfileSettings->>User: Display success message<br/>update UI with new data

    Note over ProfileSettings,SkillsAPI: Skills Update Operation
    User->>SkillsSettings: Navigates to skills settings
    activate SkillsSettings
    User->>SkillsSettings: Updates skill preferences<br/>(add/remove skills)
    SkillsSettings->>SkillsSettings: Normalize and validate skills
    SkillsSettings->>SkillsAPI: POST /api/users/change-skills<br/>body: { skills: string[] }
    activate SkillsAPI

    SkillsAPI->>SkillsAPI: Validate skills array
    SkillsAPI->>SkillsAPI: Update user skills in database
    deactivate SkillsAPI

    SkillsAPI->>SkillsSettings: Return updated skills
    SkillsSettings->>User: Display success message<br/>update skills display
    deactivate SkillsSettings

    Note over ProfileSettings,PasswordAPI: Password Change Operation
    User->>ProfileSettings: Clicks "Change Password"
    ProfileSettings->>User: Shows password change form
    User->>ProfileSettings: Enters current password<br/>new password, confirmation
    ProfileSettings->>ProfileSettings: Validate password strength<br/>and match
    ProfileSettings->>PasswordAPI: POST /api/users/change-password<br/>body: { currentPassword, newPassword }
    activate PasswordAPI

    PasswordAPI->>PasswordAPI: Verify current password<br/>using bcrypt.compare()
    PasswordAPI->>PasswordAPI: Validate new password requirements
    PasswordAPI->>PasswordAPI: Hash new password with bcrypt
    PasswordAPI->>PasswordAPI: Update password in database
    PasswordAPI->>PasswordAPI: Blacklist existing tokens<br/>(force re-login)
    deactivate PasswordAPI

    PasswordAPI->>ProfileSettings: Return success response
    ProfileSettings->>User: Display success message<br/>redirect to login
    deactivate ProfileSettings

    Note over ProfileSettings,DeleteAPI: Account Deletion Operation
    User->>ProfileSettings: Clicks "Delete Account"
    ProfileSettings->>User: Shows confirmation dialog<br/>with password requirement
    User->>ProfileSettings: Enters password and confirms deletion
    ProfileSettings->>ProfileSettings: Validate password input
    ProfileSettings->>DeleteAPI: DELETE /api/users/delete<br/>(uses user ID from authenticated token)
    activate DeleteAPI

    DeleteAPI->>DeleteAPI: Verify user password<br/>using bcrypt.compare()
    DeleteAPI->>DeleteAPI: Delete user favorites
    DeleteAPI->>DeleteAPI: Delete user profile
    DeleteAPI->>DeleteAPI: Blacklist all user tokens
    DeleteAPI->>DeleteAPI: Clear user cookies
    deactivate DeleteAPI

    DeleteAPI->>ProfileSettings: Return success response
    ProfileSettings->>User: Display account deleted message<br/>redirect to home
    deactivate ProfileSettings

    Note over User,DeleteAPI: Account management operations complete<br/>user data updated or removed
```

## Key Components & Files

### Client Side

- **ProfileSettings.jsx** (`client/src/components/ProfileSettings/ProfileSettings.jsx`)
  - Personal information management
  - Address and contact details
  - Password change interface

- **SkillsSettings.jsx** (`client/src/components/SkillsSettings/SkillsSettings.jsx`)
  - Skills preference management
  - Add/remove skill categories
  - Skills normalization

### Server Side

- **updateProfile** (`server/src/controllers/user.js`)
  - Profile information updates
  - Input validation and sanitization
  - Database updates

- **changeSkills** (`server/src/controllers/changeSkills.js`)
  - Skills array processing
  - Skills normalization and validation
  - Database updates

- **changePassword** (`server/src/controllers/changePassword.js`)
  - Current password verification
  - New password validation
  - Secure password update

- **deleteUser** (`server/src/controllers/deleteUser.js`)
  - Account deletion confirmation
  - Data cleanup and removal
  - Session invalidation

## Account Management Operations

### 1. Profile Updates

- **Personal Information**: Name, email, address
- **Avatar Upload**: Image file handling and storage
- **Contact Details**: Phone, location preferences
- **Validation**: Server-side input sanitization

### 2. Skills Management

- **Skill Categories**: Job search preferences
- **Skills Array**: Comma-separated storage
- **Normalization**: Consistent skill formatting
- **Validation**: Skill whitelist and format checking

### 3. Password Changes

- **Current Password**: Verification before change
- **New Password**: Strength requirements
- **Security**: Force logout after password change
- **Hashing**: Secure bcrypt storage

### 4. Account Deletion

- **Confirmation**: Password required for deletion
- **Data Cleanup**: Remove all user data
- **Session Termination**: Invalidate all tokens
- **Privacy**: Complete data removal

## Security Features

### Input Validation

- **Server-Side Validation**: All inputs validated server-side
- **Sanitization**: Remove malicious content
- **Type Checking**: Ensure proper data types
- **Length Limits**: Prevent buffer overflow attacks

### Password Security

- **Current Verification**: Verify existing password
- **Strength Requirements**: Same as registration
- **Secure Hashing**: bcrypt with proper salt rounds
- **Session Invalidation**: Force re-login after change

### Data Protection

- **Secure Deletion**: Complete data removal
- **Token Cleanup**: Blacklist all user tokens
- **Cookie Clearing**: Remove authentication cookies
- **Privacy Compliance**: GDPR data deletion requirements

## Error Handling

### Profile Updates

- **400 Bad Request**: Invalid input data
- **401 Unauthorized**: Authentication required
- **500 Internal Server**: Database errors

### Password Changes

- **400 Bad Request**: Weak new password
- **401 Unauthorized**: Incorrect current password
- **500 Internal Server**: Update failures

### Account Deletion

- **400 Bad Request**: Missing password confirmation
- **401 Unauthorized**: Incorrect password
- **500 Internal Server**: Deletion failures

## User Experience Considerations

### Immediate Feedback

- Real-time validation feedback
- Success/error message display
- Loading states during operations

### Security UX

- Password confirmation for sensitive operations
- Clear warnings for destructive actions
- Graceful handling of authentication failures

### Data Persistence

- Automatic saving of valid changes
- Conflict resolution for concurrent updates
- Recovery options for accidental deletions

## API Endpoints Summary

### Profile Management

- `PUT /api/users/profile` - Update profile information
- `POST /api/users/update-avatar` - Upload profile picture
- `POST /api/users/change-password` - Change password

### Preferences

- `POST /api/users/change-skills` - Update skills preferences

### Account Operations

- `DELETE /api/users/delete` - Delete account (uses authenticated user ID from token)

## Database Operations

### Updates

- Atomic profile updates
- Skills array normalization
- Password hash updates

### Deletions

- Cascade delete user data
- Remove favorites and preferences
- Clean up authentication tokens

## Security Best Practices

### Validation

- Never trust client input
- Validate all data types and formats
- Implement proper error handling

### Authentication

- Require authentication for all operations
- Verify user permissions for data access
- Implement proper session management

### Data Handling

- Use parameterized queries
- Implement proper error logging
- Follow GDPR compliance requirements

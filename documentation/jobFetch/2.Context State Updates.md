**Phase 2: Context State Updates**

- [← Back to Job Fetch Flowchart](_JobFetchFlowchart.md)
- [← Previous: Phase 1: Initial Setup & Input Validation](1.Initial%20Setup%20&%20Input%20Validation.md)
- [Next → Phase 3: Navigation](3.Navigation.md)

```mermaid
sequenceDiagram
    autonumber
    actor User
    %% File paths:
    %% SearchInput: client/src/components/SearchInput/SearchInput.jsx
    %% AlertMessage: client/src/components/AlertMessage/AlertMessage.jsx
    %% JobsContext: client/src/context/JobsContext.jsx
    participant SearchInput as SearchInput Component<br/>SearchInput.jsx
    participant AlertMessage as AlertMessage Component<br/>AlertMessage.jsx
    participant JobsContext as JobsContext Provider<br/>JobsContext.jsx

    Note over User,JobsContext: Phase 2: Context State Updates
    activate SearchInput
    SearchInput->>JobsContext: Call `setAllJobs([])`<br/>(clear previous results)
    activate JobsContext
    deactivate SearchInput

    activate SearchInput
    SearchInput->>SearchInput: Set alert state with<br/>info message:<br/>"Searching for '[inputValue]'..."
    SearchInput->>AlertMessage: Render info alert
    activate AlertMessage
    AlertMessage->>User: Display loading message
    deactivate AlertMessage
    deactivate SearchInput

    activate SearchInput
    SearchInput->>JobsContext: Call `setSearchString(inputValue)`<br/>(store search query)
    deactivate SearchInput

    activate SearchInput
    SearchInput->>JobsContext: Call `performJobFetch()` with<br/>method: POST<br/>body: { search_string: inputValue }
    Note over JobsContext: Fetch starts immediately<br/>(detailed in Phase 4.1)
    deactivate SearchInput
    deactivate JobsContext
```
